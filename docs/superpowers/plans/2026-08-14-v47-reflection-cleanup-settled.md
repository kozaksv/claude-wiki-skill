# Settled-леджер циклу v47-reflection-cleanup

Закриті вердикти циклу (споживаються движком через parseSettled; кап 12000 симв).
НЕ дублювати цю секцію в інших файлах — джерело рівно одне.

Канал передачі — НЕ файловий шлях, а `args.settled`: контролер бере вміст файлу,
на який вказує `ledgerPath` манфеста лончера, ВЕРБАТИМ (`fp-launch-args.py`:
`settled <- ledgerPath, VERBATIM`) і кладе його в аргументи рану.

## Settled

- id: r1
  verdict: ⛔
  text: Скоуп замкнений рівно: (1) перейменування одного поля блоку РЕФЛЕКСІЯ, (2) чистка `references/crystallization.md` від риштувань tier-моделі, (3) зняття мертвого фільтра `state == "active"` і чесне формулювання у `references/telemetry.md`, (4) три точкові текстові правки, (5) версія + Migration Log + тести. Це цикл ВИДАЛЕННЯ залишків, а не побудови механізмів. Нових полів, операцій, env, хук-поведінок, лічильників — НЕ додавати.
  why: цикл v46-qwen двічі втратив ран на спіралі рев'ю: кожен фікс народжував більший механізм, і наступний раунд знаходив у ньому нові P1 (вердикти s1, s3, s6). Тут дельта — переважно текст; будь-яке розростання є сигналом тієї самої спіралі.
  source: контролер циклу з повною автономією від користувача, 2026-08-14; леджер v46-qwen s1/s3/s6; фідбек-файл F16 (P0)

- id: r2
  verdict: ⛔
  text: Поле `Автоматизував:` ПЕРЕЙМЕНОВУЄТЬСЯ на `Крихталізація:` — не видаляється і не зливається з сусіднім `Зберіг у wiki:`. Не переголосовувати. Наслідок, який виводиться з цього рішення: блок РЕФЛЕКСІЯ і далі має ОБИДВА рядки, вони відповідають на різні питання (`Зберіг у wiki:` = що я чіпав під час роботи; `Крихталізація:` = чи запропонував зберегти патерн, що повторився), тож будь-яка задача, що пропонує їх об'єднати або викинути один, суперечить вердикту й має бути відхилена.
  why: механізм крихталізації живий — вирізали лише три з чотирьох його цілей (bash/python у v4.1, skill у v4.4). Живим лишився рівно один: wiki-сторінка. Питання «чи є тут щось варте збереження, щоб наступна сесія не виводила це заново» досі має сенс; неправдою стала лише НАЗВА поля, бо сторінка — не автоматизація. Мінімальна чесна правка — перейменувати, а не зносити робочий канал.
  source: контролер циклу, 2026-08-14; design-док §«Чому поле приречене на нічого»

- id: r3
  verdict: ⛔
  text: Поля `state` / `protected` / `archived_at` з `.usage.json` НЕ видаляти: форма запису на диску не змінюється, `wiki_version` лишається `"4.0"`, міграцій нуль. Змінюється рівно двоє. (а) Мертвий фільтр `state == "active"` знімається з `operation-lint.md` і `operation-wiki-status.md`. (б) `telemetry.md` перестає обіцяти v5-куратора і каже прямо: `state`/`archived_at` зарезервовані, зараз їх ніхто не пише і не читає. `protected` — ЖИВЕ поле (`wiki protect`/`unprotect`), не чіпати.
  why: видалення полів із запису = зміна форми на диску = схемна міграція з bump `wiki_version` і планом-підтвердженням для кожної існуючої вікі. Це перетворило б дешевий behavior-only цикл на дорогий схемний. А фільтр `state == "active"` шкідливий саме тим, що завжди пропускає все й через це читається як живий: жодна операція ніколи не пише інше значення.
  source: контролер циклу, 2026-08-14; замір: `grep -rn '"state"' hooks/` дає лише запис дефолту `"active"` у `post-tool-use.sh:209`

- id: r4
  verdict: ⛔
  text: Негативні гарди у `tests/skill-contracts.sh` — `set_skill_link` (:413), `writing-skills` (:418), `🧹` (:415) — НЕ прибирати: це паркан, що тримає вирізане вирізаним. Гард на ім'я поля (:427, цикл `for field in ...`) ПЕРЕНАЦІЛИТИ на нове ім'я, а не видалити. Так само лишаються рядок про скрипти в `references/maintenance-and-mistakes.md:43` і суб-сценарії-регресії у `tests/scenarios/crystallization.md`.
  why: залишок і запобіжник виглядають однаково (обидва згадують вирізане), але роль протилежна: залишок ОБІЦЯЄ неіснуюче, запобіжник ЗАБОРОНЯЄ його повернути. Знести паркан під приводом чистки — найдешевший спосіб втратити результат двох попередніх спрощень.
  source: контролер циклу, 2026-08-14

- id: r5
  verdict: ⛔
  text: `docs/specs/` і `docs/plans/` (історичні документи минулих циклів) НЕ редагувати — ні для «узгодження термінології», ні для виправлення застарілих згадок tier-моделі. Migration Log у `references/discovery-versioning.md` — тільки ДОПИСАТИ новий запис `4.7.0`; наявні записи 4.0–4.6 не переписувати. Історичну таблицю релізів у `README.md` (рядок ~370, «tiered crystallization (4 рівні зі скриптами)») теж не переписувати — це журнал того, що справді було.
  why: заборона діє з циклу v4.5 і має просту причину — історичний документ описує стан на свою дату, і його «виправлення» знищує єдиний запис про те, чому щось прибрали. Правити треба лише те, що описує СЬОГОДНІШНЮ поведінку.
  source: контролер циклу, 2026-08-14; `docs/superpowers/plans/2026-07-08-v45-hooks.md:316` (поза скоупом: редагування історичних docs/)

- id: r6
  verdict: ⛔
  text: Леджер вердиктів лишається ОКРЕМИМ файлом `docs/superpowers/plans/2026-08-14-v47-reflection-cleanup-settled.md`. Секцію `## Settled` у план НЕ переносити, не дублювати, файл не видаляти й не перейменовувати.
  why: `args.settled` = вміст `ledgerPath` ВЕРБАТИМ, тож `ledgerPath`, наведений на план, шле весь план у кожен агент-промпт. У циклі v46-qwen doc-фікс-агент видалив цей файл під приводом «винос розриває канал» — діагностика хибна, канал не файловий, а наслідок був реальний: пост-ран `--swap-pointer` упав на `paths-exist`.
  source: контролер циклу, 2026-08-14; леджер v46-qwen s2; фідбек-файл F15

- id: r7
  verdict: ⛔
  text: Періодичний нудж крихталізації і knob `nudge_interval` у цьому циклі НЕ автоматизувати. Так, після v4.5 хук PostToolUse спрацьовує на кожному Read/Edit/Write і технічно міг би рахувати ітерації замість моделі — це окремий follow-up, а не частина чистки залишків. Формулювання «модель сама себе тактує, харнес-лічильника немає» лишається чинним і правдивим.
  why: чистка мертвого і побудова нового — різні цикли. Змішування дає рівно ту спіраль, від якої захищає r1: скоуп поповз би з тексту у хук-поведінку, а хук-поведінка тягне за собою тести на трьох клієнтах.
  source: контролер циклу, 2026-08-14
