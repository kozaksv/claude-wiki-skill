# Settled-леджер циклу v46-qwen

Закриті вердикти циклу (споживаються движком через parseSettled; кап 12000 симв).
НЕ дублювати цю секцію в інших файлах — джерело рівно одне.

Канал передачі — НЕ файловий шлях, а `args.settled`: контролер бере вміст файлу,
на який вказує `ledgerPath` манфеста лончера (`.git/fp-launch/v46-qwen.json`),
ВЕРБАТИМ (`fp-launch-args.py`: `settled <- ledgerPath, VERBATIM`) і кладе його
в аргументи рану; движок парсить саме передане. Тому леджер живе окремим
маленьким файлом, а не секцією 117-кілобайтного плану.

## Settled

- id: s1
  verdict: ⛔
  text: T5 (hooks/lib/settings-lock.sh) — жодного lease-renewer, фонових процесів, нових env-перемикачів чи нових пробників живості. Буквальний перенос наявного mkdir-мутекса, «нуль зміни поведінки». Pre-existing обмеження (kill -0 EPERM cross-UID, hidepid=2/procfs, відсутність lease-продовження, TTL LOCK_MAX_AGE) — follow-up P2/P3 у леджер, НЕ blocking задачі.
  why: Кандидат рану №2 виріс у 528-рядковий lib із renewer-ом, зловив 2 security-P1 у власному новому механізмі (kill_renewer 3s bound; hidepid=2 EPERM) і ескалював NEEDS_HUMAN. Лок-файли живуть у per-user $HOME — cross-UID сценарії поза моделлю загроз скіла. Кандидата відхилено (reject bf63984), скоуп пінований у T5 п.9.
  source: контролер циклу з повною автономією від користувача, 2026-08-13, після рану wf_961d43eb-003; план T5 п.9; refs/fp-failed/2c3a30863dc5d832-v46-qwen-5

- id: s2
  verdict: ⛔
  text: Леджер вердиктів циклу лишається ОКРЕМИМ файлом docs/superpowers/plans/2026-08-13-v46-qwen-settled.md. Секцію `## Settled` у сам план НЕ повертати, не дублювати і файл не видаляти — це не стилістика, а вимога транспорту й манфеста лончера.
  why: args.settled = вміст ledgerPath ВЕРБАТИМ, тож ledgerPath на план шле 117 КБ тексту в кожен агент-промпт рану; ран №3 довів, що s1 з окремого файлу дійшов до движка (settledFp ee04e277). Фікс-агент spec-гейту (коміт 6477753) видалив файл під приводом «винос розриває канал args.settled» — хибна діагностика, канал не файловий; наслідок реальний: --swap-pointer впав на paths-exist, бо ledgerPath манфеста зник.
  source: контролер циклу з повною автономією, 2026-08-14, пост-ран протокол wf_1bd01f87-fde; манфест .git/fp-launch/v46-qwen.json (ledgerPath); fp-launch-args.py рядок 72

- id: s3
  verdict: ⛔
  text: T8 (hooks/uninstall-hooks.sh) — механізм рівно за планом п.1-4: strip_plan (read+validate+merge+tmp) → strip_commit (backup + os.replace) → відкат claude із власного backup, якщо commit(qwen) впав ЗВИЧАЙНОЮ відмовою. Заборонено: sha256-звірки pre/post digest, верифікація dev/ino tmp перед replace, повторне взяття локів заради клієнта, чия тека з'явилась посеред рану, EXIT-trap rollback за сигналом.
  why: Раунди рев'ю хвилі 4 самі породили цей механізм (c09ac5f — сигнальний rollback; 8ff4713 — digest-reconcile, dev/ino, lock-replay), і наступний раунд знайшов 6 P1 рівно в ньому: усі шість стосуються коду, доданого попередніми фіксами. Файли живуть у per-user $HOME — хто здатен підмінити tmp чи backup, той уже володіє і хуками, і скілом. Конкурентні процеси впорядковує mkdir-мутекс T5, а не звірки вмісту.
  source: контролер циклу з повною автономією, 2026-08-14, тріаж ескалації indivisible рану wf_1bd01f87-fde; чистий кандидат до спіралі — refs/fp-done/v46-qwen/t8-uninstall-hooks (41b3388); відхилено refs/fp-failed/2c3a30863dc5d832-v46-qwen-4

- id: s4
  verdict: ✅
  text: T8: сигнал (INT/TERM) у вікні між commit(claude) і commit(qwen) → чесний non-zero (130/143, а не stale $?) плюс повідомлення «повторіть uninstall», БЕЗ автоматичного відкату. Напівзнятий стан легальний і збіжний, бо uninstall ідемпотентний. Двофайлова атомарність у POSIX недосяжна: «атомарність» цієї задачі = валідація ОБОХ файлів до першого запису + відкат на звичайних відмовах, і саме так її формулює план.
  why: Правдивий код виходу — реальна вимога і лишається: uninstall.sh читає exit 0 як дозвіл видалити клон відновлення. Автоматичний сигнальний відкат — ні: він сам додав вікно «сигнал після успішного replace» (P1 №6) і потребує ще одного рівня гарантій. Ланцюг гарантій мусить закінчуватись задекларованим residual, інакше кожен фікс породжує наступний механізм із новими вікнами.
  source: той самий тріаж, 2026-08-14; знахідки P1 №1-6 рану wf_1bd01f87-fde; план T8 п.4 і п.13

- id: s5
  verdict: ✅
  text: T8 переписується КОНСТРУКТИВНО, а не забороною: весь критичний відрізок (валідація обох клієнтів + обидва записи) виконує ОДИН python-процес, який bash запускає під уже взятими локами. Tmp більше не лежить на диску між фазами. Backup лишається артефактом для людини, але джерелом істини для відкату НЕ є: відкат пише оригінальні байти З ПАМ'ЯТІ процесу у свіжий mkstemp і робить os.replace. Чотири окремі python-виклики (plan×2, commit×2) скасовано.
  why: Дві P1 рану №4 (підміна tmp між plan і commit; довіра rollback до backup-шляху) існують саме тому, що фази були окремими процесами, а стан жив на диску між ними. Один процес прибирає довге вікно, а відкат із пам'яті прибирає читання backup узагалі — знахідки зникають структурно, без жодної digest/inode-звірки. Це не поступка рев'юеру і не ігнорування його: підстава для знахідки усувається конструкцією.
  source: контролер циклу з повною автономією, 2026-08-14, тріаж settled-конфлікту рану wf_a8c4190b-046 (contradictsSettled=s3, 2×P1 codex-атк); план T8 п.1/п.2/п.14; кандидат 78dde3f0 показав bash-оркестрацію з 4 викликів

- id: s6
  verdict: ⛔
  text: Залишкове вікно між os.close(tmp) і os.replace(tmp, settings) У МЕЖАХ одного процесу — задекларований residual (спека R14), а не дефект: rename(2) у Python адресує шлях, renameat2/RENAME_NOREPLACE недоступний, тож жодна перевірка перед replace вікна не закриває — вона лише зсуває його. Нові механізми заради нього не додаються: ані dev/ino, ані digest, ані утримання fd «як гарантії».
  why: Рев'юери циклу вже дали P1 обом крайнощам: «перевірки нема» (ран №4) і «перевірка є, але fd закрито до replace, тож вікно лишилось» (ран №3, P1 №3/№4). Обидві претензії вірні й одночасно нездійсненні — це ліміт мови, а не задачі. Ланцюг гарантій закінчується тут: непередбачуване mkstemp-ім'я, per-user тека, ідемпотентний повторний запуск.
  source: той самий тріаж, 2026-08-14; спека R14; знахідки рану wf_a8c4190b-046 і P1 №3/№4 рану wf_1bd01f87-fde
